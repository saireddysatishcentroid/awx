---
 - hosts: all
   tasks:
   - name: Clone a github repository
     git:
       repo: https://{{gituser}}:{{gitpass}}@github.com/centroidcloudteam/ms-tools.git
       dest: /tmp/
       clone: yes
       update: yes
